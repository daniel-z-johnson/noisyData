# Data source config
spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/noise
        username:
        password:
        driver-class-name: org.postgresql.Driver
    jpa:
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        show-sql: true
        hibernate:
            ddl-auto: create

